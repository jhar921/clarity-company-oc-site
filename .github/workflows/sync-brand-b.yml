name: sync-brand-b
on:
  push:
    paths:
      - .github/workflows/sync-brand-b.yml
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Decode embedded site
        run: |
          set -e
          python3 - <<'PY'
          import base64, pathlib
          # favicon-32 only as smoke - real restore via MCP deploy
          print('placeholder workflow - use MCP deploy')
          PY
      - name: noop
        run: echo skip
